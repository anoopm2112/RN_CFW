import { createIconSetFromFontello } from 'react-native-vector-icons';
import fontelloConfig from '../icons/fontello/config.json';

export default FontelloIcon = createIconSetFromFontello(fontelloConfig);
